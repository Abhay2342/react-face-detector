import React from "react";

function Navigation() {
    return (
        <nav style={{display: 'flex', justifyContent: 'flex-end'}}>
            <p className="f3 b link dim black underline pr3 pointer">
                Sign Out
            </p>
        </nav>
    )
}

export default Navigation;